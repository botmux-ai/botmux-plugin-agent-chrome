{
  "name": "@botmux-ai/plugin-agent-chrome",
  "version": "0.2.1",
  "description": "Agent Chrome Stack plugin for botmux.",
  "type": "commonjs",
  "keywords": [
    "botmux-plugin"
  ],
  "scripts": {
    "build": "node ./scripts/build.mjs",
    "validate": "node ./scripts/validate.mjs",
    "test": "npm run build && npm run validate && node ./test/check-cursor-scale.mjs && node ./test/check-runtime-home.js && node ./test/check-launcher-runtime.js && node ./test/check-session-context.js && node ./test/check-broker-binding.js && node ./test/check-vnc-password.js && node ./test/check-dashboard-copy.js",
    "prepublishOnly": "npm test"
  },
  "devDependencies": {
    "@novnc/novnc": "1.7.0",
    "chrome-devtools-mcp": "1.5.0",
    "esbuild": "^0.28.0",
    "puppeteer-core": "^25.2.1",
    "react": "^19.2.7",
    "ws": "^8.18.0"
  },
  "botmux": {
    "schemaVersion": 1,
    "id": "agent-chrome",
    "displayName": "Agent Chrome",
    "service": {
      "mode": "auto"
    }
  },
  "files": [
    "dist/"
  ],
  "publishConfig": {
    "registry": "https://registry.npmjs.org/",
    "access": "public"
  },
  "license": "MIT"
}
